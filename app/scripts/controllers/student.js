'use strict';

angular.module('tesisApp')
  .controller('StudentCtrl', function ($scope,$cookieStore,$location) {
  	/*if(String($cookieStore.get('id')) != user || String($cookieStore.get('pass')) != pass){
  		$location.path('/');
  	}
  	else{
  		console.log('hola');
  	}
    */
  });
